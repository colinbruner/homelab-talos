# https://github.com/prometheus-community/helm-charts/blob/main/charts/kube-prometheus-stack/values.yaml

# Override in favor of generic 'monitoring' namespace
namespaceOverride: monitoring

# We disable Grafana as part of the kube-prometheus deployment as 
# we manage the Grafana instance using the Grafana operator.
grafana:
  enabled: false