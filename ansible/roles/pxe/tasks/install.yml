---

- name: install directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
  loop: "{{ directories }}"

- name: install required packages
  ansible.builtin.apt:
    pkg: "{{ packages }}"