# Values

This directory contains values for individual node (control or worker) configurations.

## Schema
The [schema.yml](./schema.yml) file acts as a schema representation for `ytt` to generate configurations.

For more info, check out [ytt](https://carvel.dev/ytt/).